"""Conflict registry package.

A `Conflict` is the dot-on-the-globe entity in the new pipeline; this package
holds the hand-curated YAML registry, the canonical actor catalog, and the
loader that parses + validates them into ORM-ready dicts.
"""
